##[
This package conains several submodules:

- `curly`, means `{}` based format (Python's str.format)
- `percent`, means `%` based format (Python's `str.__mod__`)
]##
import ./pyformats/[curly, percent]
export curly, percent

